import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.droidsans.com", "upload.wikimedia.org", "cdn.pixabay.com", "picsum.photos"],
  },
};

export default nextConfig;
